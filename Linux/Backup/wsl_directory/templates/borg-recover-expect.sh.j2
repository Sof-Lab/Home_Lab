#!/usr/bin/expect -f

set timeout -1
spawn ./borg-recover.sh
expect {
        "*Are you sure you want to continue connecting*" { send -- "yes\r" }
        "Enter*passphrase*" { send -- "{{ passphrase }}\r" }
}
expect {
        "Enter*passphrase*" { send -- "{{ passphrase }}\r" }
}
expect {
        "Enter*passphrase*" { send -- "{{ passphrase }}\r" }
}
expect eof
